[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA redirect: tutte le rotte vanno a index.html (React Router)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
